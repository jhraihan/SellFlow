# ---------------------------------------------------------------- core
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3

# ---------------------------------------------------------------- auth
djangorestframework-simplejwt==5.3.1

# ---------------------------------------------------------------- api docs
drf-spectacular==0.28.0

# ---------------------------------------------------------------- config
python-decouple==3.8
dj-database-url==2.3.0

# ---------------------------------------------------------------- database
# Postgres driver, used on Render. Local dev falls back to SQLite.
psycopg[binary]==3.2.3

# ---------------------------------------------------------------- serving
gunicorn==23.0.0
whitenoise==6.8.2

# ---------------------------------------------------------------- media
# Required by Django's ImageField (store logos, product images).
Pillow==11.0.0

# ---------------------------------------------------------------- security
# Fernet encryption for courier credentials at rest (PRD FR-2.5)
cryptography==44.0.0
